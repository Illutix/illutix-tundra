version: '3.8'

services:
  polars-server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=development
      - ALLOWED_ORIGINS=["http://localhost:3000"]
    volumes:
      - ./data:/app/data
    restart: unless-stopped